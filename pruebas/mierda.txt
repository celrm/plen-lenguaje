import source from file.

type matriz = arr\arr\ent.

const ent pi = 3.

function my_quick return arr\ent (arr\ent v) {
    arr\ent w = quicksort(v).
    return w.
}

main {
ent patata = 1.

ent a = kin().
    while (a) {
    }    
}
